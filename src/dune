(library
  (name        bst)
  (public_name bst)
  (modules bisec_tree)
  (libraries batteries))

(executables
  (names   test dump)
  (modules test dump)
  (libraries unix dolog batteries minicli bst))
